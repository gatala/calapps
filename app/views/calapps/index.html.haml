-#  This file is app/views/movies/index.html.haml
%h1 Cal Applications

/ %table#calapps
/   %thead
/     %tr
/       %th Application Title
/       //%th URL
/       %th Creator(s)
/       %th Description
/       %th More Information
/   %tbody
/     - @calapps.each do |app|
/       %tr
/         %td= link_to "#{app.name}", app.URL
/         //%td= link_to "#{app.URL}", app.URL
/         %td= app.creator
/         %td= app.description
/         %td= link_to "More about #{app.name}", calapp_path(app)

%table.table.table-striped
  %thead
    %tr
      %th= "Name"
      //%th= model_class.human_attribute_name(:URL)
      %th= "Creator(s)"
      %th= "Description"
      //%th= model_class.human_attribute_name(:created_at)
      %th= "More Information"
  %tbody
    - @calapps.each do |calapp|
      %tr
        //%td= link_to calapp.id, calapp_path(calapp)
        %td= link_to "#{calapp.name}", calapp.URL
        //%td= calapp.URL
        %td= calapp.creator
        %td= calapp.description
        //%td= calapp.created_at
        %td
          = link_to t('.view', :default => t("helpers.links.More Details")), calapp_path(calapp), :class => 'btn btn-mini'

%p
= link_to t('.new', :default => t("helpers.links.new")), new_calapp_path, :class => 'btn btn-primary'