%h1 Calapps Users

%table
  %thead
    %tr
      %th Username
      %th Email
  %tbody
    - @users.each do |user|
      %tr
        %td= link_to "#{user.name}", user_path(user)
        %td #{user.email}

