%h1 Update Account

= form_for @user do |f|

  = f.label :name, 'Name'
  = f.text_field :name

  = f.label :email, "Email"
  = f.email_field :email
  
  .form-actions
    = f.submit 'Update', :class => "btn btn-primary"
    = link_to 'Back', user_path, :class => "btn"
